<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.allu.bank">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.MainActivity" />
        <activity android:name=".UI.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.SignUpActivity" />
        <activity android:name=".UI.CustomerDetails.CustomerMainActivity" />
        <activity android:name=".UI.CustomerDetails.ViewCustomersActivity" />
        <activity android:name=".UI.CustomerDetails.AddCustomerActivity" />
        <activity android:name=".UI.CustomerDetails.SearchCustomerActivity" />
        <activity android:name=".UI.AnamolousDetails.AnamolousMainActivity" />
        <activity android:name=".UI.AnamolousDetails.AnamolousViewActivity" />
        <activity android:name=".UI.AnamolousDetails.AnamolousSearchActivity" />
        <activity android:name=".UI.TransactionDetails.TransactionMainActivity" />
        <activity android:name=".UI.TransactionDetails.TransactionViewActivity" />
        <activity android:name=".UI.TransactionDetails.TrasactionSearchActivity"></activity>
    </application>

</manifest>